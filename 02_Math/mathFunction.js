let a = 3;
let b = 81;
let c = -10;
let x = 3.49;
let y = 7;
let z;

//math functions
z = Math.random; //generates random number between 0 and 1
z = Math.floor(x); //rounds down
z = Math.ceil(x); //rounds up
z = Math.round(x); //rounds to closest integer
z = Math.PI; //gives the value of PI
z = Math.E; //gives the value of Euler's number
z = Math.trunc(x); //removes decimals
z = Math.pow(a, y); //raises the power i.e 3 to the power of 7
z = Math.sqrt(b); //returns square root of the number
z = Math.abs(c); //returns the absolute value i.e if the number is -ive, make it +ive

//useful
let max = Math.max(a, b, c); //returns the biggest number
let min = Math.min(x, y, z); //returns the smallest number
